# @mainsquare/excalidraw-math

## Install

```bash
npm install @mainsquare/excalidraw-math
```

If you prefer Yarn over npm, use this command to install the Excalidraw utils package:

```bash
yarn add @mainsquare/excalidraw-math
```

With PNPM, similarly install the package with this command:

```bash
pnpm add @mainsquare/excalidraw-math
```
